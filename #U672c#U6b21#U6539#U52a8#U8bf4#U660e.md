# 本次改动说明

本次改动包含两部分：**取消每天定时向企业微信发送简报**，以及**新闻去重优化**（附带若干附加优化）。所有既有单元测试保持通过，并新增了针对去重优化的回归测试。

---

## 一、取消每天定时企微简报（已彻底删除相关代码）

每天早上 7 点（北京时间）向企业微信 Webhook 推送每日简报的功能已完全移除：

- **`core/pipeline/main_pipeline.py`**：删除 `from core.notifier import maybe_send_news_notification` 引入，以及流水线末尾调用 `maybe_send_news_notification(...)` 的整段 try/except 逻辑。抓取/去重/渲染主流程不受影响。
- **删除文件**：
  - `core/notifier.py`（简报发送核心，已无引用）
  - `scripts/notifier.py`（转发 shim）
  - `notifier.py`（根目录遗留旧版副本，无任何引用）
  - `tests/test_notifier.py`（简报相关测试）
- **`.github/workflows/update-news.yml`**：从 “Update data” 步骤移除仅供每日简报使用的环境变量 `WEBHOOK_URL`、`WEBHOOK_TYPE`、`WEBHOOK_MODE`、`WEBHOOK_DAILY_HOUR`、`WEBHOOK_HOTNESS_THRESHOLD`。

### 需要你注意的一点
工作流里仍保留了 **“Notify on failure” 步骤**（仅在 CI 构建失败时通过 `WEBHOOK_URL` 推送一条告警）。它不是“每日简报”，而是运维健康告警，只在流水线失败时触发。为避免影响你的故障监控，本次予以保留。**如果你希望连这条失败告警也一并去掉**，告诉我即可，我会删除该步骤及其对 `WEBHOOK_URL` 的依赖。

> 部署侧：若你在部署环境/CI Secrets 中不再需要 `WEBHOOK_*`，可自行清理；保留也不会再触发每日推送。

---

## 二、新闻去重优化（`core/dedup/deduplicator.py`）

针对“仍存在相同/高度雷同内容”的问题，在**不改变任何既有公开函数行为**（`normalize_title_for_dedup`、`compute_title_similarity`、`_pick_best_item`、`merge_items_group`、`dedupe_items_by_title_url` 等签名与语义均保持向后兼容）的前提下，做了以下增强：

### 1. 更强的 URL 归一化（Layer 1 召回）
新增 `_canonical_dedup_url()` 作为去重键（不影响展示 URL）。在原有去除 utm/ref 等追踪参数基础上，额外统一：
- `http` / `https` 视为同源；
- 去除 `www.` / `m.` / `amp.` / `mobile.` 等等价子域前缀；
- 去除路径尾部的 `/amp`、`index.html`、`default.html` 与多余斜杠。

这样同一篇文章的多种链接写法会被归并为同一条。

### 2. 子集/超集标题的“包含”合并（Layer 3 召回）
新增 `compute_containment()`：当较短标题的字片几乎完全被较长标题覆盖（包含度 ≥ 0.90、较短标题 ≥ 8 字、长度比 ≥ 0.45）时判为重复。用于捕捉如「标题A」与「标题A（附完整评测/实测）」这类因长度差异被原 bigram Jaccard 长度剪枝漏掉的重复。

### 3. 描述佐证的跨源合并（Layer 3 召回）
当两条标题中度相似（≥ 0.5）但未达默认阈值，且**正文/描述高度一致**（bigram Jaccard ≥ 0.80）时判为同一内容。用于捕捉不同来源对同一事件改写标题的重复。

### 4. 倒排索引分桶，去重性能优化（Layer 3 性能）
模糊层改用 **bigram 倒排索引**做候选分桶（blocking）：每条新闻只与“共享至少一个 bigram”的少量候选比较，将原本 O(n²) 的两两比较降为近线性。由于任意两条真正相似/包含的标题必然共享大量 bigram，召回不受影响。

### 防误合保护
以上召回增强都设有严格门槛（最小长度、长度比下限、高包含度阈值、描述强佐证），避免把不相关新闻或短/通用标题误并。新增的 `tests/test_dedup_enhanced.py` 专门覆盖：URL 归一化、包含合并、描述佐证合并，以及“无关新闻不合并”“短通用标题不误合”等保护用例。

---

## 三、验证

```bash
python -m pytest tests/ -v      # CI 环境（已装 feedparser/fastapi/pytest）
# 或本地仅验证去重/工具模块：
python -m unittest tests.test_dedup tests.test_dedup_enhanced tests.test_utils -v
```

本次改动涉及的 `test_dedup`、`test_dedup_enhanced`、`test_utils` 共 28 项全部通过。


---

## 四、无用/冗余代码清理（第二轮：删除 + 合并）

基于引用图分析（所有生产入口与 API 均只依赖 `core/*`，`scripts/*` 只被测试引用），做了如下清理：

### 1. 删除确实无用的死文件
- `normalizer.py`（根目录）—— 无任何引用，与 `core/normalize/normalizer.py` 重复。
- `scratch/analyze_dupes.py`、`scratch/test_filter.py`（整个 `scratch/` 目录）—— 开发调试用的临时脚本，无任何引用。

### 2. 合并去除 `scripts/*` 向后兼容“转发层”（单一数据源归一到 `core/*`）
以下文件均为纯粋 `from core.X import *` 的转发 shim，已删除，并将测试中的引入改为直接指向 `core/*`：

| 已删除的 shim | 归一到的正式模块 |
| --- | --- |
| `scripts/dedup.py` | `core.dedup.deduplicator` |
| `scripts/models.py` | `core.models` |
| `scripts/utils.py` | `core.utils` |
| `scripts/output.py` | `core.output` |
| `scripts/archive.py` | `core.archive` |
| `scripts/logging_config.py` | `core.logging_config` |
| `scripts/translate.py` | `core.normalize.translator` |
| `scripts/topic_filter.py` | `core.normalize.normalizer` |
| `scripts/ai_processor.py` | `core.agents.analyst_agent` |
| `scripts/fetchers/`（整目录） | `core.fetch.*`（opml/waytoagi/builders/newsletters/aggregators/aihub；`official`→`rss_fetcher`；`oss_trending`→`github_fetcher`） |

### 3. 合并分岔的重复实现：recommend
- `scripts/recommend.py`（178 行，旧版）与 `core/recommend.py`（183 行，生产在用的新版）仅在末尾 `enrich_recommendation_fields` 的评分逻辑上分岔（新版保留 Signal Score 2.0 引擎分，仅在未评分时回退到紧凑分）。已删除旧版 `scripts/recommend.py`，测试改为验证正式的 `core/recommend.py`。

### 保留的真实入口脚本（未动）
- `scripts/update_news.py`（CI 主入口，导入 `core.pipeline.main_pipeline`）
- `scripts/prerender.py`（CI 预渲染，仅依赖标准库）
- `scripts/__init__.py`（包标记）

> 影响说明：若有外部代码曾 `from scripts.xxx import ...`，需改为对应的 `core.*` 路径（见上表）。项目内部（core/api/入口脚本/测试）已全部更新。

### 验证
去重/工具/recommend/抽取器/主题过滤 等可本地运行的测试共 62 项全部通过，`compileall` 全量通过；代码与配置中无任何残留的 `scripts.*` shim 引用。
